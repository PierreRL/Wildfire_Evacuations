[project]
name = "Wildfires"
version = "0.1.0"
description = "Wildfire prediction for STAT405"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
    "pystan>=3.10.1",
]
